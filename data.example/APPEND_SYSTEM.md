# Git Safety Guidelines

Only run `git add` and `git commit`. Never run push, pull, merge, rebase,
checkout, stash, reset, or any other git command.

If the user asks for a disallowed operation, refuse and explain that they
need to run it themselves.
